{
	"type": "module",
  "name": "cyclopedia-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node copy-html.js",
    "serve": "vite preview"
  },
  "dependencies": {
    "firebase": "^9.0.0"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  }
}
