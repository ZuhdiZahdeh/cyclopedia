{
	"type": "module",
	"name": "cyclopedia-app",
	"version": "0.0.0",
	"scripts": {
		"dev": "vite",
		"build": "vite build && node copy-html.js",
		"deploy-clean": "npm run build && node clean-copy.js && firebase deploy"
		"serve": "vite preview"
	},
	"dependencies": {
		"dotenv": "^16.5.0",
		"firebase": "^9.23.0",
		"firebase-admin": "^13.4.0",
		"node-fetch": "^3.3.2"
	},
	"devDependencies": {
		"vite": "^4.0.0"
	}
}
