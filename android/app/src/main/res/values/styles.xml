<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- الثيم الأساسي للتطبيق (AppCompat DayNight بدون ActionBar) -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- احذف هذه العناصر لو لم تُعرِّف ألوانًا -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- نسخة بدون ActionBar (للتوافق) -->
    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- الثيم المستخدم على الـ Activity حسب الـ Manifest -->
    <style name="AppTheme.NoActionBarLaunch" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <!-- Fallback لأي مرجع إلى Theme.SplashScreen -->
    <style name="Theme.SplashScreen" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

</resources>
